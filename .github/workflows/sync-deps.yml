name: 📦 Dependency consistency

on:
  pull_request:
  push:
    branches:
      - main

jobs:
  sync-deps:
    runs-on: ubuntu-latest
    steps:
      # 1️⃣ Check out your code
      - uses: actions/checkout@v3

      # 2️⃣ Install pnpm (optional, but recommended for pnpm workspaces)
      - uses: pnpm/action-setup@v2
        with:
          version: 8

      # 3️⃣ Install all dependencies
      - run: pnpm install

      # 4️⃣ Run Syncpack’s linter to catch any version mismatches
      - name: Check for version mismatches
        run: pnpm run sync:check
